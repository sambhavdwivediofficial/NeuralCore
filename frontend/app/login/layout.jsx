// app/login/layout.jsx

export const metadata = {
  title: 'SignIn',
  description:
    'Sign in to NeuralCore and access your AI workspace.',
};

export default function LoginLayout({ children }) {
  return children;
}
